# ビルド (vite build) のときだけ読まれる。開発サーバーには効かない。
#
# 手元で開発する間は同じホストの 8787 を見てほしいので、こちらに分けてある。
# 秘密は入っていない (接続先の名前だけ) ので、これは追跡してよい。
#
# /v1 を付けているのは、後から作りを変えたときに古いクライアントを
# 動かし続けられるようにするため。剥がすのは Caddy の仕事で、
# サーバー側のコードは自分がどの経路で呼ばれたかを知らない。
VITE_SERVER_URL=wss://mgohttp-server.pepaga.me/v1
