import { t } from '../../i18n'
import './Loading.css'

/**
 * 読み込み中の覆い。
 *
 * --- なぜ隠すか ---
 * 揃う前の戦場は**壊れているように見える**。地形はブロックアウトの箱だけで
 * 立っていて、自分の模型は素の姿勢 (T ポーズ) のまま。どちらも「まだ届いて
 * いない」という意味なのに、画面からはそう読めない。
 *
 * 見せないほうが正直になる。**回っている印が「待てば来る」を伝える**ので、
 * 壊れた戦場を見せて不安にさせるより短く済む。
 *
 * --- なぜ塗り潰すか ---
 * 半透明にすると、箱だけの地形と T ポーズが透けて同じことになる。ここは
 * 隠すための覆いなので、下地ごと塗る (Blocked と同じ扱い)。
 */
export default function Loading() {
  return (
    <div class="loading">
      {/* 回る印。**文字より先に読める** — 止まっていないことが一目で分かる */}
      <div class="loading-ring" />
      <div class="loading-note">{t('hud.loading')}</div>
    </div>
  )
}
